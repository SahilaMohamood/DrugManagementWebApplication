@model IEnumerable<DrugManagementWebApp.Models.AntiAllergicDrug>

@{
    ViewData["Title"] = "Index";
    PagerModel pager = ViewBag.Pager;
}


<h1>Anti-Allergic Drug</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div class="row">
    <div class="col-12">
        <partial name="_topBar" model="@pager" />
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AntiAllDrugShortName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AntiAllDrugDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AntiAllDrugShortName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AntiAllDrugDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AntiAllergicDrugId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AntiAllergicDrugId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AntiAllergicDrugId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<div class="row">
    <div class="col-12">
        <partial name="_bottomBar" model="@pager" />
    </div>
</div>
