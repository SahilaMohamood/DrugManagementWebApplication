@model IEnumerable<DrugManagementWebApp.Models.AllergicSymptom>

@{
    ViewData["Title"] = "Index";
    PagerModel pager = ViewBag.Pager;
}


<h1>Allergic Symptom</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div class="row">
    <div class="col-12">
        <partial name="_topBar" model="@pager" />
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AllSymName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AllSymDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AllSymName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AllSymDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AllergicSymptomId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AllergicSymptomId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AllergicSymptomId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<div class="row">
    <div class="col-12">
        <partial name="_bottomBar" model="@pager" />
    </div>
</div>
